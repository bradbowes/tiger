/*
   Copy stdin to stdout until end of file
*/

/*
let
   ch = getchar()
in
   while ch <> chr(-1) do
      begin
         putchar(ch);
         ch := getchar()
      end
end
*/

let
   copy(ch: char) =
      if ch <> chr(-1) then begin
         putchar(ch);
         copy(getchar())
      end
in
   copy(getchar())
end
